import React from "react";
import Board from "../components/Board";

const Menu = () => {
  return (
    <div style={styles.menu}>
      <p>This is a menu</p>
    </div>
  );
};

export default Menu;

const styles = { menu: { textAlign: "center" } };
